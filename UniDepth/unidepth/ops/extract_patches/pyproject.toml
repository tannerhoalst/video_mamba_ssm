[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "RandomPatchExtraction"
version = "0.1.0"
description = "CUDA random patch extraction op for UniDepth"
requires-python = ">=3.8"
authors = [{ name = "Luigi Piccinelli" }]
